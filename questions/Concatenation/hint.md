> We can loop trough an array of elements using a loop
> ```py 
> for x in arr_1:
>    print(x)
> for x in arr_2:
>    print(x)
> ```

> We can append an element to a list using its `.append() method`sele