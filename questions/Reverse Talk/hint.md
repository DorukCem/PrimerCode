> Extracting the words from the sentence into a collection such as a list would probably be helpful

> The built in `.split()` method on strings can used to split it into parts

> Strings can be reversed using the slice syntax `reverse = s[::-1]`